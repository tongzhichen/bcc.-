
Call you mine 2019/11/26 14:20:59
#include <stdio.h>
int main()
{
    unsigned int a,b;
    double d;

    char c;

    a = 0;
    b = 0;

    while((c = getchar()) != ' ') {
            a = a*10 + c - '0';
        }
    

    while((c = getchar()) != ' ') {
            b = b*10 + c - '0';
        }

    c = getchar();
    switch(c) {
    case '+' :
        d = a+b;
        break;
    case '-':
        d = a-b;
        break;
    case '*':
        d = a*b;
        break;
    case '/':
        if(b == 0) {
            printf("Error: connot divide a number  by 0");
            break;
        }
        else{d =1.0*a/b;}
        break;
        }

    if(b!=0) {
        printf("The expression has a value of : %.1f\n",d);
    }

    
    return 0;
}

Call you mine 2019/11/26 14:21:05
第二个

Call you mine 2019/11/26 14:21:29
#include <stdio.h>
int main(void)
{
   char c;
   int i ;
   int j;
   i = 0;
   j = 0;
   printf("Enter any characters not more than 50 characters:");
   while(( c = getchar() )!= '\n')
   {
       if ( (c == 'a')||(c == 'A'))
       {
           i++;
       }
       else if ( c == '0')
       {
           j++;
       }
    }
       printf("the number of a is %d\n the number of 0 is %d",i,j);
       return 0;
}
         第三个
# include <stdio.h>
int main(void)
{
    unsigned long long int x,y,b,a,g;
     x = 0;
    y = ~x;
    g = ~y;

    a = 1;
    b = 0;
    while(a!=0)
    {
        a = a<<1;
        b = b+1;
    } 
    printf("unsigned long long int is size of %llu bit",b);
     printf("The max is %llu,the min is %llu",y,g);
    return 0;
}
